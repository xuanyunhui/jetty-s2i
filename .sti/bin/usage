
#!/bin/bash -e
cat <<EOF
This is the docker.io/jetty S2I image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

sti build git://<source code> docker.io/jetty <application image>

You can then run the resulting image via:
docker run <application image>
EOF
